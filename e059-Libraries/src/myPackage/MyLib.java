package myPackage;

public class MyLib
{
    // Evaluate if a number is even
    public static boolean isEven (int a)
    {
        return a%2 == 0;
    }

    // Increases a number 50%
    public static double increaseHalf (int a)
    {
        return (a + (a/2));
    }
}
